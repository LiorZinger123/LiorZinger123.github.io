apiVersion: v1
kind: Service
metadata:
    name: my-service
spec:
    selector:
        app.kubernetes.io/name: lior-client
    ports:
        - protocol: TCP
          port: 8080
          targetPort: 4173
